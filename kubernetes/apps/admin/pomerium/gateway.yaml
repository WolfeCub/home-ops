---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: default
  namespace: pomerium
spec:
  gatewayClassName: pomerium-gateway
  listeners:
    - name: https
      hostname: "*.joshwolfe.ca"
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs:
          - name: "${domain/./-}-tls"

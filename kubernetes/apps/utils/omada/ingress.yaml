---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: omada
  namespace: utils
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`omada.${domain}`)
      services:
        - name: omada
          port: 8043
  tls:
    passthrough: true

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: omada-tls
  namespace: utils
spec:
  secretName: omada-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "omada.${domain}"
